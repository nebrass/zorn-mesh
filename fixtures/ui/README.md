# Local UI fixtures

`quality-readiness.json` is the stable Story 6.9 evidence snapshot emitted by
`apps/local-ui/src/quality-gates.ts`. It records accessibility, responsive,
browser, offline-asset, and critical-journey fixture results for release
readiness checks.
